{% include 'includes/header.twig' %}
<body class="bg-gradient-to-r from-purple-500 to-blue-500">
<nav class="bg-opacity-80 p-4">
    <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-4xl font-bold text-white"><a href="/episodes">R&M Episodes</a></h1>
        {% include 'includes/weather.show.twig' %}

        <div class="relative flex ml-auto">
            {% include 'includes/weather.set.twig' %}

            {% include 'includes/episode.search.twig' %}

        </div>

    </div>
</nav>
<div class="container mx-auto p-6">
    <h1 class="text-3xl font-bold text-center text-gray-100">{{ episode.name }}</h1>
    <p class="text-lg text-center text-gray-300 my-3">Air Date: {{ episode.airDate|date('F d, Y') }}</p>
    <p class="text-lg text-center text-gray-300 my-3">Episode: {{ episode.episode }}</p>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 mt-4">
        {% for character in episode.characters %}
            <div class="bg-white p-4 shadow-lg rounded-lg cursor-pointer transform hover:scale-105 transition duration-300">
                <img src="{{ character.image }}" alt="{{ character.name }}" class="w-32 h-32 mx-auto mb-4 rounded-full">
                <p class="text-xl font-semibold text-gray-800 text-center">{{ character.name }}</p>
                <p class="text-sm italic text-center">Species: {{ character.species }}</p>
                <p class="text-sm italic text-center">Gender: {{ character.gender }}</p>
                <p class="text-sm italic text-center">Origin: {{ character.origin }}</p>
            </div>
        {% endfor %}
    </div>
</div>

{% include 'includes/footer.twig' %}
